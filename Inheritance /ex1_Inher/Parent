package Inheritance;

public class Parent {
	
	
	// 상속
	// 두 개 이상의 클래스와 부모와 자식을 개념으로 관계를 맺는 것
	// 상속의 대상은 변수와 메소드이다. 나머지는 대상이 아니다
	// 상속에 상속이 가능하다
	// 단일 상속(클래스 한 개)만 가능하다
	// 모든 클래스는 Object클래스를 상속받는다
	

	int age;
	
	void add(int x,int y) {
		System.out.println(x+y);
	}
}
